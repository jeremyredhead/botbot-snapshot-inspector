# BotBot Snapshot Inspector
(*or some thing like that IDK a better name*)

Pretty straightforward to use, just open in a recent web browser, and load the desired BotBot JSON snapshot files.

TODO:
- add folder upload support via good ol' `webkitdirectory` attribute
- add `.tar.gz` support maybe if a good JS tar library can be found
- column filtering, collapsing, better sorting
- don't overflow off the damn page
- checkbox selecting bots, generate list of UUIDs (i.e. for "keep/discard these bots")
  - allow adding notes on bots to enhance this purpose
